# Project Worklog

---
Task ID: 1
Agent: Super Z (main agent)
Task: GPX Repair & Reconstruction Web App — planning stage only (inspect repository, analyze requirements, design architecture, produce master development plan; NO implementation)

Work Log:
- Inspected `/home/z/my-project`: greenfield git repo (branch `main`, 1 initial commit, clean tree); no application code, no package.json; only `download/`, empty `upload/`, git-ignored `skills/`.
- Verified toolchain: Node v24.21.0, npm 11.19.0, bun 1.3.14; `.env` contains only DATABASE_URL (unused by this product — serverless app).
- Classified task: planning stage of a Type 3 (interactive web dev) project; per user instruction, no fullstack-dev initialization, no scaffolding, no code written.
- Designed full architecture: layered feature-oriented client architecture (presentation / Zustand state / pure-TS domain / infra adapters); original-vs-reconstructed provenance enforced at type level (discriminated unions + `Estimated<T>`); original data immutable; merged view derived.
- Made technology decisions: MapLibre GL JS v5 + OpenFreeMap tiles; custom drawing layer (no mapbox-gl-draw); internal Vincenty geodesy module (golden-tested); native DOMParser/XMLSerializer GPX facade; Zustand; elevation via provider abstraction with OpenTopoData primary (opt-in, reconstructed coords only) and Terrarium tiles as reserved fallback; Vitest + RTL + Playwright testing stack.
- Defined GPX pipeline (ingest→parse→validate→detect gaps→reconstruct→merge→export→download), timestamp case matrix (4 cases incl. no-time fallback), hysteresis-based elevation gain/loss, honesty rules for statistics.
- Produced 12-phase incremental plan (Phase 0 foundation → Phase 11 release prep), each with objective/scope/tasks/files/dependencies/tests/acceptance/DoD/non-goals; deferred backlog fenced (road-snapping, freehand, etc.).
- Persisted deliverables: `docs/MASTER_PLAN.md` (815 lines) + copy at `download/MASTER_PLAN.md`.

Stage Summary:
- Deliverable: complete master plan (sections 0 + A–P + closing note) at `/home/z/my-project/docs/MASTER_PLAN.md` and `/home/z/my-project/download/MASTER_PLAN.md`.
- Key decisions: 100% client-side core (no backend/DB — scaffold DB layer to be removed at Phase 0 with advance notice); network egress limited to map tiles + opt-in elevation lookups; export invariant = original point values byte-identical.
- Repository state: unchanged (no code, no new commits) — implementation intentionally NOT started per user instruction.
- Next step: await user instruction to begin Phase 0 (or plan revisions).

---
Task ID: 2
Agent: Super Z (main agent)
Task: Phase 0 — Foundation & Tooling Baseline (user-authorized; implement ONLY Phase 0, no product features)

Work Log:
- Loaded fullstack-dev skill; ran mandatory init script → scaffolded Next.js 16 + TS strict + Tailwind 4 + shadcn/ui (+ Prisma, demo API route, websocket examples — to be pruned per plan).
- Pruned per pre-announced Phase 0 scope: removed `src/app/api/` (demo route), `prisma/`, `src/lib/db.ts`, `examples/`, `mini-services/`, all `db:*` scripts; `bun remove prisma @prisma/client next-auth next-intl z-ai-web-dev-sdk` (server-coupling deps, unused by serverless app). Removed stale `db/custom.db` artifact; untracked `.env` (scaffold convention: gitignored).
- Quality guards ON: `typescript.ignoreBuildErrors: false`, `reactStrictMode: true`, tsconfig `noImplicitAny: true`; tsconfig exclude scoped to project (skills/ excluded — environment tooling, not app code).
- `output` stays `"standalone"` (sandbox deployment contract) — static-exportability verified in ISOLATED copy instead (env forbids `bun run build` in live workspace): `.build-verify/` with output:'export' → `next build` PASS, `out/index.html` produced, all routes static, type-checked during build. Script persisted at `scripts/verify-static-export.sh`.
- Added tooling: vitest 5 (`vitest.config.mts`, @-alias, node env), Playwright 1.63 (`playwright.config.ts`, no webServer — targets system dev server on :3000, Chromium project), ESLint flat-config boundary rules per MASTER_PLAN §F (maplibre-gl isolation to lib/map, components↛features, domain purity for features/lib-geo, page.tsx composition rule; dormant until those dirs exist).
- Wrote Phase 0 app shell: `src/app/page.tsx` (semantic header/main/footer, responsive, sticky footer, no features), `src/app/layout.tsx` (product metadata, remote favicon URL removed for privacy).
- Tests: `tests/architecture.test.ts` (6 guards: no api routes, no server actions, no prisma/db wiring, strict TS, no ignored build errors, strict mode) + `e2e/smoke.spec.ts` (3 specs: shell renders, mobile 360px, zero console errors).
- Verification run: lint PASS (0 problems), typecheck PASS (exit 0), unit 6/6 PASS, E2E 3/3 PASS (Chromium vs live dev server), static-export isolated build PASS, agent-browser verification PASS (shell renders, a11y tree correct, zero page errors, network = localhost only incl. self-hosted font). Screenshot: `download/phase0-shell-verification.png`.
- Fixed during testing: JSDoc `**/*.test.ts` glob terminated block comment early (ESLint parse error) → rephrased; vitest config renamed `.ts`→`.mts` (native ESM loader, removes Vite warning); tsconfig include += `**/*.mts`.
- Committed as `phase(0): foundation and tooling baseline`.

Stage Summary:
- Clean, guarded, serverless baseline: Next.js 16 + TS strict + Tailwind 4 + shadcn/ui, zero product features, zero server routes, zero DB.
- All quality gates green: lint / typecheck / unit / E2E / static-export verification / browser check.
- Known cosmetic: `/favicon.ico` 404 (no favicon asset; branding deferred to Phase 11). `tests/database-runtime-build.sh` (environment infra script) now stale — references removed `db:push`; left untouched as sandbox tooling, not project code.
- Next: await user authorization for Phase 1 — GPX Domain Core (no UI).


---
Task ID: 3
Agent: Super Z (main agent)
Task: Phase 1 — GPX Domain Core (no UI). Implement ONLY Phase 1 per user authorization: parse, validate, gap-detect, and re-emit GPX as pure, fully-tested TypeScript.

Work Log:
- Reviewed Phase 0 state (clean tree, `phase(0)` commit): Next.js 16 + TS strict + Tailwind 4 scaffold, eslint boundary rules dormant-but-active for features//lib/geo, vitest (node env), playwright smoke, isolated static-export verification script, dev server on :3000.
- Re-read MASTER_PLAN §E-2/E-3/F/G/H/N-1 and the Phase 1 chapter; confirmed scope: types/domain.ts, lib/geo geodesy+bbox, features/gpx {parse,validate,detectGaps,exportGpx}, provenance schema defined-but-unused, fixture corpus + generators.
- Probed jsdom 30 XML behaviors via throwaway spike (deleted after): parsererror root format "L:C: msg", subtree serialization DOES include inherited xmlns declarations, serialize∘parse is idempotent, BOM tolerated, children skips comments/PIs, getAttribute decodes entities. These findings shaped the parser/exporter design.
- Design decision (ESLint domain purity vs §E-3 native DOMParser): created `src/lib/utils/xml.ts` as the ONLY DOM-global touchpoint (XmlIo interface + createDomXmlIo factory); features/gpx receive XmlIo by injection — pure & testable under jsdom without global mocking. Migrated scaffold utils.ts → utils/index.ts (git mv, plan's §F target structure; `@/lib/utils` specifier unchanged for all 44 existing importers).
- Implemented types: ids.ts (branded ids + deterministic constructors t{N}s{M}, `${seg}:${i}`, gap/{before}/{after}); domain.ts (full §G vocabulary incl. Estimated<T>, SourceKind, Reconstruction/DrawVertex/TimeStrategy/ReconstructedPoint/MergedPointView for later phases, plus documented Phase-1 extensions: RawTrkptCapture for byte-identity, anchored extras, GpxParseError/ParseOutcome, ValidationIssue vocabulary).
- Implemented lib/geo: geodesy.ts (Vincenty inverse WGS-84 + haversine fallback for non-convergent/degenerate cases, coincident→0, NaN propagation, polylineLengthMeters, hasFiniteCoords) and bbox.ts (bboxOf skipping non-finite, unionBBox).
- Implemented features/gpx/parse.ts: namespace-tolerant (default/1.0/prefixed/no-ns via localName + explicit structure walk), typed hard errors (malformed-xml w/ line-col, not-a-gpx-document, invalid-version), NaN-guarded numbers, strict ISO-8601-with-tz incl. SEMANTIC date validation (V8 Date.parse rolls Feb 30 over — caught by tests, fixed with explicit range checks), verbatim raw capture (attrs incl. null-vs-absent, ele/time texts, extra children snapshots, anchored segment/track/root extras, wpt/rte snapshots), parse-time flags (invalid-coord/invalid-ele/unreliable-time/zero-coord) + aggregated issues, GPX-1.0 root-metadata fallback, BOM tolerance, deep-freeze outside production.
- Implemented validate.ts: flag-enrichment on a COPIED model (input never mutated), relational checks (out-of-range coords, ele window, time-reversed, dup, geodesic speed-spike w/ configurable threshold), structural checks (empty/single-point segment, track-without-segments, no-timing-data), bounded aggregated issues, parse issues carried through without duplication.
- Implemented detectGaps.ts: per-track point streams, three candidate kinds (time-gap >120s, speed-anomaly >25km/h with >10s Δt guard, segment-break), intrinsic boundary dedupe with kind priority + max severity, severity ranking (severe/suspect/info, doc order within rank), implied distance/speed diagnostics via shared geodesy, flag-aware evidence (invalid/out-of-range coords excluded, negative Δt immune).
- Implemented provenanceSchema.ts: gpxr namespace/elements/attributes + builders (documented as defined-but-unused until Phase 7; tested to lock the vocabulary; round-trip suite asserts identity export emits NO gpxr markers).
- Implemented exportGpx.ts: identity round-trip — root attrs from raw (absent creator stays absent), version-matched namespace (1.0/1.1), 1.0 root-level vs 1.1 metadata layout, verbatim point emission (attrs + ordered children incl. extras via fragment parse+import), anchored extras replay, wpt/rte snapshot re-emission, declaration + LF, documented normalizations (schema-order layout, CDATA→text, comments dropped).
- Fixtures: 29 committed .gpx files covering the full §N-1 matrix (dialects, damage, gaps, unicode, BOM, CDATA, Garmin ext, anchors) + generators.ts (mulberry32 seeded, 100k-point capable, gap injection, withTime/withEle switches, XML escaping).
- Tests: 9 files / 149 tests — geodesy goldens (Flinders-Buninyong 54972.271m, analytic equator/meridian arcs, antipodal fallback, properties), parser corpus (every fixture → expected model/error), validator (flags, issues, immutability, idempotence), gap detection (exact-at-limit boundaries via deterministic impliedSpeed readback, guard, ranking, merging), round-trip (whole-corpus identity + byte-identity + export stability + no-gpxr + 5k synthetic), fixtures/generator determinism, provenance schema, xml adapter guards, Phase-0 architecture guards.
- Bugs found & fixed during testing: generator metadata <time> ignored withTime; V8 Date.parse accepts 2024-02-30 (rollover) → added semantic range validation; unterminated regex in a test; wrong expectation for mixed-anomalies (Δt guard correctly suppresses 3s zero-coord jumps — that damage is the validator's finding, not a gap; test now documents both behaviors); duplicate deepFreeze extracted to features/gpx/deepFreeze.ts.
- Verification: typecheck PASS; lint PASS (added coverage/** to eslint ignores — generated artifacts); vitest 149/149 PASS; coverage 97.91% lines overall on types/lib-geo/features-gpx/lib-utils-xml (threshold 90; per-module ≥92.85% except documented impossible defensive line); isolated static-export build PASS (out/index.html); Playwright smoke 3/3 PASS; agent-browser check: shell renders, zero page errors, console clean, network localhost-only.
- Committed as `phase(1): gpx domain core`.

Stage Summary:
- Domain core complete and fully tested: typed parse→validate→detectGaps pipeline + identity exporter, zero UI, zero runtime deps added (jsdom + @vitest/coverage-v8 are devDeps for the test suite).
- Core invariant verified by tests: every original point's lat/lon/ele/time values round-trip byte-identically; export never mutates the model; export∘parse is stable.
- Known normalizations documented in exportGpx.ts header (schema-order emission, CDATA→text, comments/PIs dropped inside tracks).
- Deviations from §G sketch (all documented in domain.ts/parse.ts headers): raw-capture structures for byte-identity; PointAnomaly extended with parse-local kinds; ids re-exported from domain.ts.
- Next: await user authorization for Phase 2 — Upload & Inspection UI (zustand install, sessionStore, useGpxSession, upload/report/gap-list components, original-only stats).

---
Task ID: 4
Agent: Super Z (main agent)
Task: Phase 2 — Upload & Inspection UI. Implement ONLY Phase 2 per user authorization (plus the user's codebase-architecture/reuse rules): upload a GPX, see validation report, segments, detected gaps, original-only statistics; zustand session state; threshold settings with persistence; error/empty states. Non-goals: map rendering, editing, elevation, export UI.

Work Log:
- Re-inspected repo before writing code (user rule §17): Phase 0+1 committed, clean tree; zustand already a scaffold dependency (no install needed — "add zustand" satisfied by verifying existing one); RTL/jest-dom missing → installed as devDeps (@testing-library/react, @testing-library/dom, @testing-library/jest-dom).
- Reused Phase 1 domain wholesale: parseGpx/validateGpx/detectGpx pipeline, GapThresholds + DEFAULT_GAP_THRESHOLDS, geodesy primitives, fixtures corpus + generators, gpxTestUtils helpers. Zero reimplementation of domain logic.
- New domain modules (pure, in coverage scope): features/statistics/distance.ts (per-segment + file totals; legs = within-segment consecutive pairs; damaged-coordinate legs excluded AND counted with reason breakdown via shared isUsableStatsPoint predicate — reused by hook for recorded extent), features/statistics/time.ts (moving/wall/gap/untimed/reversed buckets; legs = same-track consecutive pairs spanning segment breaks, aligned with detectGaps boundaries so recorded+gap spans ≈ wall time).
- New lib/utils/format.ts (§L-1 centralized formatting: distance m/km-2dp, duration m:ss / h:mm:ss, speed km/h, locale datetime, 5-dp coords) — 100% line-covered.
- State layer: state/session-store.ts (idle/loading/parsed/error lifecycle; frozen model stored wholesale, gaps replaceable via setGaps for pure re-detection), state/ui-store.ts (gap thresholds persisted to localStorage v1 key, partialize settings-only, skipHydration + AppShell mount rehydrate → no SSR mismatch).
- hooks/use-gpx-session.ts: the orchestration hook — File#text → parseGpx(createDomXmlIo) → validateGpx → detectGaps; empty-file guard; typed-error → SessionError mapping (describeParseError); threshold-change effect re-runs detection on unchanged model; memoized view models (GapRow/SegmentRow joined with resolved boundary points; DistanceStats/TimeStats; BBox extent); re-exports domain result types as the app-layer facade so components never import @/features (ESLint §F boundary).
- Components (props in / intents out, no domain imports): layout/{app-shell (composition root, 'use client', rehydrates settings), header (New file action), panel-grid (3-col responsive), map-placeholder (Phase 3 slot + textual extent)}; gpx/{upload-zone (drag/drop/picker, privacy promise, re-selectable input), gpx-summary-card (identity/counts/no-timing badge), validation-report (severity-sorted, kind labels), segment-list (track-grouped rows), gap-threshold-settings (popover, blur/Enter commit — avoids per-keystroke O(n) re-detection), session-error-alert}; reconstruction/gap-list (textual rows: kind/severity/elapsed/coords/diagnostics); statistics/{stats-panel (provenance column mandatory, "—"+reason honesty rules, exclusion footnotes), provenance-badge (Recorded/Estimated/Mixed vocabulary; only Recorded used in Phase 2)}.
- page.tsx reduced to pure composition (imports components/layout only per ESLint rule); vitest config extended (.test.tsx include; coverage scope += features/statistics + lib/utils/format).
- Tests: unit (stats-distance 9, stats-time 11, format 13) + RTL (upload-zone 5; inspection-flow 9: valid→summary/gaps/stats, malformed/not-gpx/bad-version/empty errors, no-timing mode, threshold re-run, reset, damaged-segments disclosure). Fixed during testing: distance leg-accounting bug (pairs after a bad point now counted as excluded — found by hand-computed test), test-side BASE() helper arity bug, no-time assertion wrongly including distance cell, popover toggle-closed-on-second-click test bug (blur keeps popover open by design).
- Duplication review (user rule §19): amber/destructive severity styling was inlined 3× → consolidated into components/shared/status-badge.tsx (tones danger/warning/success/neutral; vocabularies stay local per component). Verified single sources: geodesy (lib/geo only), time buckets, formatting, error mapping, view-model builders, threshold definitions.
- E2E: smoke.spec.ts updated to Phase 2 shell; new upload-inspection.spec.ts (6 specs): valid→gaps listed, malformed→actionable error, not-gpx, no-timing mode, threshold change + localStorage persistence across reload + reset-to-defaults, 100k-point generated fixture (seeded, gitignored) parses without freeze (3.8s incl. upload; gap detected; UI stays interactive). 
- Verification: typecheck PASS; lint PASS (fixed react-hooks/immutability hit — SegmentList closure mutation → pure grouping); unit+RTL 196/196 PASS; coverage 98.2% lines overall (statistics/format 100%, threshold 90); E2E 9/9 PASS; isolated static-export build PASS; agent-browser live verification: hero + workspace render, upload flow, threshold re-run both directions, empty-file error path, damaged-file report, mobile 360px, sticky-footer-on-short/natural-push-on-overflow, zero page errors, network localhost-only. Screenshots: download/phase2-{workspace,mobile,damaged-file}-verification.png.
- Committed as `phase(2): upload and inspection UI`.

Stage Summary:
- Phase 2 acceptance criteria all verified: valid file → summary + gaps; invalid files → precise errors; no-timestamp file → "no timing data" mode; settings changes re-run detection (+ persistence).
- Architecture intact: page.tsx 12 lines (composition only); ESLint boundaries hold (components → hooks/state/types/lib-utils only); features/statistics pure; no duplicate domain logic (consolidated StatusBadge).
- Deviations from plan §F naming: kebab-case files matching scaffold convention (use-gpx-session.ts, session-store.ts); components/shared/ added per user's architecture rules (StatusBadge); GapThresholdSettings placed under components/gpx (detection is an inspection concern in Phase 2; GapList under components/reconstruction per §F).
- Known deferrals: pace statistics + pace formatting (Phase 5), map (Phase 3), MobileSheet (Phase 3), parse Web Worker (Phase 9).
- Next: await user authorization for Phase 3 — Map Display.

---
Task ID: 5
Agent: Super Z (main agent)
Task: Phase 3 — Map Display. Implement ONLY Phase 3 per user authorization (plus the user's codebase-architecture/reuse rules): interactive MapLibre map with recorded route, per-segment styling, gap highlight + boundary markers, fit-bounds (activity + per-gap), tile-provider config (OpenFreeMap default / OSM raster / blank fallback), attribution, legend, responsive layout, graceful offline degradation, GapList ↔ map selection sync. Non-goals: drawing, reconstruction rendering, elevation profile.

Work Log:
- Re-inspected repo before writing code (user rule §17): phases 0–2 committed, clean tree; surveyed useGpxSession view models, isUsableStatsPoint, BBox utilities, format utils, StatusBadge, fixtures/generators — all reused, zero domain reimplementation.
- Installed maplibre-gl@6.11.1 (plan said v5; v6 is the current major with identical properties per §E-1 criteria and a compatible API surface — named exports, same setStyle/fitBounds/attributionControl shapes; documented deviation).
- Worker strategy (v6 resolves its worker at runtime via import.meta.url-relative siblings, which bundlers cannot preserve): vendored `maplibre-gl-worker.mjs` + `maplibre-gl-shared.mjs` into public/vendor/ via `scripts/sync-maplibre-worker.mjs` (postinstall hook + manifest with SHA-256). Controller pins them with `setWorkerUrl("/vendor/...")`. A unit test asserts public copies match node_modules byte-for-byte (upgrade-without-sync fails tests, not the map).
- New lib/map (sole maplibre importer, ESLint boundary): styles.ts (provider registry — OpenFreeMap vector default, OSM raster with usage-policy note, blank local fallback; type-only maplibre import, node-test-safe), geojson.ts (pure builders: route lines / gap spans / boundary markers → GeoJSON FeatureCollections, [lon,lat] order), mapController.ts (the imperative wrapper: dynamic `import("maplibre-gl")` → prerender-safe, code-split [1.1 MB chunk absent from initial HTML — verified in static-export output]; 8 gpxr-* layers incl. transparent hit layers, severity-tinted dashed spans, before=ring/after=dot markers + selection casing/halo; click/hover selection via layer-scoped events; responsive-padding fitBounds with lastCameraAction bookkeeping; provider switch re-adds layers on style.load preserving camera; offline model: style-fetch failure → BLANK_STYLE fallback + sticky notice, tile errors post-load → notice, retry + switch clear it; WebGL failure → 'unsupported' status; ResizeObserver; window.__gpxMapController test bridge gated to non-production; StrictMode-safe create/destroy).
- ui-store: added persisted tileProvider setting + transient selectedGapId (partialize excludes selection; backward-compatible with Phase-2 localStorage).
- hooks/use-map-controller.ts: React binding — receives the GpxSession view models (single source; nothing recomputed), subscribes uiStore slices, owns controller lifecycle (create on parsed-mount, destroy on reset — new file recreates), drives setRoute/setTileProvider/highlightGap/fitBounds effects; selection hygiene (clears stale ids on reset/re-detect); exposes MapBinding facade (types re-exported so components never import lib/map). Pure exported buildRouteView: solid line pieces split at gap boundaries AND unusable points (isUsableStatsPoint — the single shared usability predicate), dashed spans + boundary markers per gap (skipped when a boundary is damaged — the line hole is the honest signal), usablePointCount for the a11y summary.
- New components (props in / intents out): map/{map-canvas (container + status overlays: initializing/unsupported/offline/empty-route + sr-only textual alternative §C-5), map-toolbar (provider popover with policy notes + fit-activity), map-legend (solid/dashed/ring/dot encodings — color-never-alone), gap-highlight-overlay (selected-gap chip)}; shared/gap-vocabulary.tsx (GAP_KIND_LABELS + GapSeverityBadge — consolidated OUT of gap-list per rule 5/19 before the map chip would have duplicated them). GapList: rows became toggle buttons (aria-pressed, data-selected, scrollIntoView) wired to the shared selection. AppShell composes useGpxSession + useMapController and distributes selection. MapPlaceholder deleted (superseded; its textual-extent role moved into MapCanvas fallback + sr-only summary).
- react-hooks/refs (new in eslint-config-next 16) taints any member expression flowing into a ref attribute: fixed architecturally with a stable callback ref (setContainer) passed as a separate MapCanvas prop (verified via probe files — the sanctioned pattern, rule left ON). ESLint ignores += public/vendor/** (minified vendored assets had produced ~1100 warnings).
- vitest config: css.postcss emptied inline (component tests importing maplibre-gl.css otherwise dragged the Tailwind/PostCSS pipeline in and crashed); maplibre CSS aliased to a stub; coverage scope += lib/map/{styles,geojson}.ts (controller is browser-only → E2E).
- Tests: map-styles (registry + vendor byte-sync guard), map-geojson (builders), map-route-view (line splitting at gaps/damage, span/marker emission, unusable-boundary skips, whole-corpus sanity incl. zero-coords/mixed-anomalies), map-components (RTL: all MapCanvas states with fake binding, toolbar, legend, chip, GapList selection toggle), inspection-flow updated (map-canvas + jsdom WebGL fallback path). E2E map-display.spec.ts (7 specs): layers via bridge, list→map focus, map-click→list sync (real mouse click at projected span position after camera settles), mobile viewport, blocked tiles → offline notice + overlays still rendered + app functional, provider switch both directions, 50k fixture pan (real drag) + zoom responsiveness.
- Fixed during testing: test expectation that would have connected a line across out-of-range damage (the design forbids it — corrected test, not code); getTestState crashed when v6 getStyle() transiently returns undefined during style swaps → null-guarded + pollBridge catches transient bridge rejections; PopoverClose added to provider options (popover previously stayed open → strict-mode locator violation); map-click E2E read stale span positions during the fit animation → added `moving` flag + CAMERA_SETTLED predicate.
- Verification: typecheck PASS; lint PASS; unit 230/230 PASS; coverage 98.24% lines overall (lib/map pure modules 100%, threshold 90); E2E 16/16 PASS (7 map + 9 Phase 2); isolated static-export build PASS (out/index.html; maplibre chunk lazy — not in initial HTML); agent-browser live verification: upload → OpenFreeMap render, map-click selection both directions, deselect, provider switch with layers intact, blocked-tile offline (blank fallback + notice + retry recovery after unblock), mobile 375px, zero page errors, clean console. Screenshots: download/phase3-{map-verification,gap-selected,offline-degradation,mobile-verification}.png.
- Committed as `phase(3): map display`.

Stage Summary:
- Phase 3 acceptance criteria all verified: route visible with distinct gap styling and markers; selecting a gap focuses the map (list→map and map→list); pan/zoom smooth on 50k-point fixture; attribution visible.
- ARCHITECTURE REPORT (user rule §20):
  - Reused existing code: useGpxSession view models (data/gapRows/extent — no recomputation in the map layer); isUsableStatsPoint as the single usability predicate (extent, distance stats, and route rendering share one definition); lib/geo BBox; lib/utils/format; StatusBadge; shadcn Popover/Button/Card; zustand stores; fixture corpus + seeded generators; gpxTestUtils pipeline helpers.
  - New modules: lib/map/{styles,geojson,mapController}, hooks/use-map-controller (buildRouteView + MapBinding), components/map/{map-canvas,map-toolbar,map-legend,gap-highlight-overlay}, components/shared/gap-vocabulary, scripts/sync-maplibre-worker (+public/vendor assets), e2e/map-display.spec, 4 unit/RTL test files.
  - Reuse decisions: MapBinding consumes GpxSession rather than re-subscribing stores (single source of truth; AppShell stays the composition root per the App.tsx rule); route-view building lives in the hook layer (app) so lib/map stays infra-pure while domain predicates flow downward; gap vocabulary centralized in shared/ only because two features now consume it (map chip + gap list) — rule 7 boundary respected; PopoverClose added to the scaffold wrapper instead of a parallel popover implementation.
  - Duplication check: gap kind/severity vocab single-sourced (gap-vocabulary.tsx; validation-report's maps are the distinct ValidationIssueKind vocabulary, not duplicates); maplibre confined to lib/map (verified by grep — no component or feature imports); worker assets generated by script with hash manifest (no hand-maintained copies); no repeated bbox/padding/format logic outside their single homes.
  - Architecture impact: lib/map now exists as the isolation boundary the plan specified (§F-4); the hook-facade pattern (types re-exported through hooks/use-map-controller) keeps components off restricted imports; the controller's imperative API (setRoute/highlightGap/fitBounds/startDrawSession-ready surface) is the seam Phase 4's draw editor will extend; ui-store gains its second persisted setting group and its first transient selection state; test bridge + blank fallback style are reusable test infrastructure for Phases 4–8 E2E.
- Known deviations: maplibre-gl v6 (not v5 — current major, same decision criteria; named exports); kebab-case filenames (Phase 2 convention); MapCanvas receives the ref-callback as a separate prop (react-hooks/refs constraint, documented in-file).
- Next: await user authorization for Phase 4 — Reconstruction Editor: Drawing.

---
Task ID: 6
Agent: Super Z (main agent)
Task: GitHub publish setup — configure repo identity to the owner's GitHub account, clean history of session artifacts, create the remote repository, and push the full project (phases 0–3) under the owner's name.

Work Log:
- Received owner's GitHub PAT; verified identity via GitHub API: Hachiiki (Willard James Paluga); set repo-local git identity to `Willard James Paluga <90497788+Hachiiki@users.noreply.github.com>` (GitHub noreply address — guarantees attribution to the account).
- Audited history before any push: all 8 commits were authored by agent identities (`Z User <z@container>`, `Super Z <dev@gpx-repair-studio.local>`); two commits contained only `tool-results/` session artifacts; the planning commit carried a UUID message; the initial commit included a `.env` (local SQLite path only — no credentials, but scrubbed on principle).
- Rewrote history via `git filter-branch`: scrubbed `tool-results/` and `.env` from all commits, pruned the two artifact-only commits (now empty), reworded planning commit to `docs: architecture master plan and phased roadmap`, reset author/committer identity on every commit to the owner. Pre-rewrite state preserved locally in `refs/original/`.
- Verified post-rewrite: 6 commits (Initial → docs master plan → phase(0)…phase(3)) all under the owner's identity with original dates intact; tree diff vs pre-rewrite = tool-results deletions only; `.env` absent from publishable history; secrets scan (ghp_/github_pat_ patterns) clean.
- Restored `tool-results/` to disk (runtime cache) and added `/tool-results/` to `.gitignore` so environment auto-commits can never re-track session artifacts.
- Created GitHub repository `Hachiiki/gpx-repair-studio` via API and pushed `main`.

Stage Summary:
- All commits and pushes now carry the owner's identity; remote authenticated with the owner's PAT (embedded in the local remote URL — rotates with the token).
- Publishable history is clean: phase-conventional commit messages, no session artifacts, no env files, no secrets.
- Next: Phase 4 — Reconstruction Editor: Drawing, awaiting user authorization.

---
Task ID: 7
Agent: Super Z (main agent)
Task: Phase 4 — Reconstruction Editor: Drawing. Implement ONLY Phase 4 per user authorization (plus the user's codebase-architecture/reuse rules): draw and edit the missing route for a selected gap with full undo/redo; pure command-stack draw model + resample + snap; editor store with per-gap repairs, skip marks, and derived gap status; map draw session (vertex mode, anchor snapping, optional snap-to-original-points, rubber band, handles for move/insert/delete); live distance badge; distinct reconstruction rendering; DrawEditorPanel + UndoRedoBar; gap status transitions; straight-line warning; explicit Draw/Pan toggle. Non-goals: timestamps/pace, elevation, export, freehand mode, gesture hardening (Phase 8).

Work Log:
- Re-inspected repo before writing code (user rule §17): phases 0–3 committed, clean tree; reused useGpxSession/GapRow joins, isUsableStatsPoint, geodesy (polylineLengthMeters), format utils, StatusBadge, shadcn primitives, fixture corpus, test bridge pattern — zero domain reimplementation.
- Pure domain first (features/reconstruction/): drawModel.ts — invertible command values (insert/move/delete/set; append = insert-at-end so inverses are strictly symmetric), pure applyDrawCommand with MAX_VERTICES=128 hard cap + defensive guards, DrawHistory stack (commit pushes undo + clears redo; undo applies inverse; redo re-applies; no-ops on empty), reconstructionDistanceMeters (anchors always included), straight-line heuristic via new geodesy cross-track. resample.ts — resamplePath builds [before, vertices, after] with spherical-interpolation fill at ≤ spacing; cumulative distances measured true-geodesic per pushed point; anchors/vertices never moved (repair only inserts). snap.ts — buildSnapCandidates (anchors + usable original points in the inflated gap box, bounded) + nearestSnap (haversine magnet, anchors beat ordinary points).
- geodesy.ts extended (the single distance module): crossTrackDistanceMeters (spherical, signed, coincident-track fallback) + interpolateLatLon (spherical nlerp, antimeridian-safe) — moved out of resample when the controller's midpoint handles needed the same primitive (one implementation, two consumers).
- state/editor-store.ts: activeGapId/drawMode/snapEnabled (transient), reconstructions per GapId (immutable replace), skippedGapIds, per-session history + monotonic vertexSeq (ids never reused). Gap status DERIVED (deriveGapStatus: in-progress > skipped > reconstructed > new), never stored. openEditor unskips (editing implies repairing); skip of the active gap closes its editor; prune drops state for gaps removed by re-detection; reset on new file.
- lib/map/mapController.ts draw session: startDrawSession/updateDrawSession/endDrawSession + setDrawMode (disables dragPan/doubleClickZoom/boxZoom, crosshair cursor — the risk-table #1 Draw/Pan contract). Pointer interactions: canvas click → snapped add (guard: no add on handle/midpoint hits or trailing drag-click — suppress flag); handle mousedown → transient drag override → mouseup commits ONE snapped move command; midpoint-handle click → snapped insert; dblclick on handle → delete. Rubber band last-path-point → cursor (hidden while dragging). Snap resolved via injected DrawSnapFn (pixel→meter threshold measured by unprojecting 100px — no tile math, no domain code in the adapter). Gap-span selection clicks suppressed while drawing (no camera refits mid-draw). New layers: gpxr-recon (dashed emerald committed), gpxr-draft-line/-rubber/-handle/-midpoint + transparent hits. Test bridge: drawSession snapshot (path, handle/midpoint screen positions, rubber state), projectLatLon/unprojectXY.
- hooks: use-map-controller — buildRouteView extended with ReconstructionRenderRef[]: a gap with vertices loses its unknown span; committed gaps render the resampled path (markers stay as recorded↔authored seams); the ACTIVE gap suppresses the span but renders no line (the draft owns it). MapBinding.getController exposed for the draw hook. use-draw-editor.ts — session lifecycle (reset on new file, prune on re-detection), snap-magnet injection (candidates memoized per gap, snapEnabled read at event time), controller driving (start/update/end/setDrawMode), derived binding (distance, straight-line warning, statusById, undo/redo counts, cap).
- Components: shared/gap-vocabulary + GAP_STATUS_* (new vocabulary beside the kinds/severities; StatusBadge gained an "info" tone — extending the shared renderer); reconstruction/{draw-editor-panel (context, live distance + provenance badge, cap surfacing, straight-line Alert, settings row: spacing select + snap toggle, vertex rows with delete = the accessible path, skip/done/close), undo-redo-bar}; map/{draw-distance-badge (role=status, aria-live), map-toolbar Draw/Pan segmented toggle (session-gated), map-canvas chrome wiring, map-legend recon entries}; gap-list: per-row GapStatusBadge + "Draw route / Edit route" action; rows extracted to GapRowItem with a transition-only scrollIntoView useEffect (the old inline-ref variant re-scrolled on every re-render — fixed a real bug found by E2E: the page scrolled mid-drawing and moved the canvas under the pointer). app-shell composes useDrawEditor and distributes the binding.
- Tests: 9 new/extended files. Unit: draw-model (constructor validation, cap, pure application incl. defensive no-ops, stack invariants, strict inversion involution, undo/redo apex round-trip — geometryRevision documented as a change counter so undo bumps it), resample (roles/ids, short-leg no-fill, equatorial 12-interval golden with per-leg ≤ spacing, monotonic cumulative ≈ true polyline length, interpolation goldens), snap (candidates/anchors/damaged exclusion/bounding, threshold + anchor priority), editor-store (full state machine: open/add/insert/move/delete/clear/undo/redo/skip/prune/reset, id non-reuse, cap through the store, settings-never-commands, immutability of prior snapshots), geodesy +crossTrack/+interpolate goldens, map-draw-view (span replacement, anchor connection exactness, densification, active-flag semantics, new GeoJSON builders). RTL: draw-editor-panel (all presentation states + a store-integration harness driving open→add→undo→redo→clear through the real store), map-components extended (badge, Draw/Pan toggle intents, canvas draw chrome, sr-summary announcement; RouteViewData literals updated).
- E2E draw-editor.spec.ts (6 specs): anchors-connected-exactly (path endpoints == boundary points, live badge growth, rubber band, immutability: recorded route + stats unchanged, span yields to draft), undo/redo/clear, drag+midpoint-insert+row-delete+undo, commit→reopen→skip transitions (span returns, "Edit route"/"Reconstructed"/"Skipped"/"Editing" badges), Draw/Pan gating, mobile 375px drawing.
- Fixed during testing: resample fill loop interpolated from the previous FILL instead of the leg's start node (positions clustered near the leg end — caught by the equatorial golden; the interpolation base is now pinned); add-vertex removed from the command union (its inverse asymmetry broke invertibility — append is insert-at-end); canvas selector typo (.maplibre-canvas vs .maplibregl-canvas) cost a debugging round; GapRowItem scroll fix (above); active-gap span suppression (reopen previously resurrected the unknown span under the draft); playwright workers set to 1 after the kernel OOM-killer took down the dev server and renderer tabs under 4GB sandbox memory with 6 (and 2) parallel WebGL workers — documented in playwright.config.ts.
- Verification: typecheck PASS; lint PASS (one react-hooks/refs hit fixed architecturally — snap cache synced in an effect, never during render); vitest 315/315 PASS; coverage 96.88% lines overall (drawModel 91.08, resample 100, snap 100, geodesy 98.11; threshold 90); E2E 22/22 PASS serial (7 map+draw Phase 4 additions); isolated static-export build PASS; agent-browser live verification: upload → draw 2 vertices (rubber band + live badge 143 m), undo 2→1, redo 1→2, commit (recon line renders, span replaced, recorded route untouched, gap row "Reconstructed"/"Edit route"), zero page errors, mobile 375px drawing. Screenshots: download/phase4-{draw-editor-session,committed-reconstruction,mobile-drawing}.png.
- Committed as `phase(4): reconstruction drawing editor`.

Stage Summary:
- Phase 4 acceptance criteria all verified: draw → edit → undo → redo → clear functional; distance updates live (badge + panel, Estimated-provenanced); reconstruction visibly distinct (dashed emerald + legend); anchors always connected (path endpoints asserted exactly in unit + E2E); vertex hard cap enforced at the domain boundary (constructors return null; apply guards defensively).
- ARCHITECTURE REPORT (user rule §20):
  - Reused existing code: useGpxSession view models + GapRow joins (the draw hook consumes the same rows as the map and the list); isUsableStatsPoint as the single usability predicate (snap candidates exclude exactly what stats/extent/rendering exclude); geodesy primitives (polylineLengthMeters for live distance; haversine for the magnet); lib/utils/format; StatusBadge; ProvenanceBadge ("estimated" variant finally consumed); shadcn primitives; zustand patterns from the existing stores; test-bridge + pollBridge E2E infrastructure; fixture corpus.
  - New modules: features/reconstruction/{drawModel,resample,snap}.ts (pure domain); state/editor-store.ts; hooks/use-draw-editor.ts; lib/map draw-session extension (geojson builders + controller interaction layer); components/reconstruction/{draw-editor-panel,undo-redo-bar}.tsx; components/map/{draw-distance-badge}.tsx; shared vocabulary extension; 9 test files; e2e/draw-editor.spec.ts.
  - Reuse decisions: interpolation lives in geodesy (moved from resample when a second consumer appeared — one implementation rule); snap resolution is injected into the controller as a function so lib/map stays free of feature imports (dependency direction preserved); the controller's draw session is the Phase-3 seam extended (setRoute/highlightGap/fitBounds untouched); gap status is a derived join, not stored state — detection results stay pure; spacing/time-strategy are settings (§D-3.5), never commands; per-gap history dies with the editor session (undo never spans sessions).
  - Duplication check: one command stack (drawModel), one path builder (resamplePath — map rendering and future Phase 5/7 consume it), one snap vocabulary, one status vocabulary (gap-vocabulary.tsx — the map chip, gap list, and editor panel share it); severity/danger styling still single-sourced in StatusBadge; no distance math outside geodesy (grep-verified); the draft path in the controller intentionally recomputes from session state (interaction-owned transient rendering, documented as such — the authoritative geometry is the store's).
  - Architecture impact: features/reconstruction now exists as the third feature domain (pure, coverage-scoped); the editor store establishes the pattern for repair state (keyed by deterministic gap ids, prunable, reset-on-new-file); MapBinding.getController opens a sanctioned controller-access channel for sibling hooks (components still cannot import lib/map); the active-flag render ref documents the draft/committed split that Phase 5 (timestamps over the same path) and Phase 7 (merge/export from the same reconstructions map) will build on; E2E bridge gained projectLatLon/unprojectXY — reusable for Phase 5+ assertions.
- Known deviations: MAX_VERTICES=128 and SNAP_RADIUS_PX=14 are Phase-4 constants (tunable later via settings if users ask); drag-commit snapping applies once at mouseup (no live snap preview during drag — documented in-controller); Playwright runs serially in this sandbox (memory budget, config-documented).
- Next: await user authorization for Phase 5 — Time & Pace Reconstruction.

---
Task ID: 8
Agent: Super Z (main agent)
Task: Manual-testing support — user asked "how can I test everything we have done?" and clarified the push workflow (commit + push per BIG change, not batched per phase). Deliver small, friendly demo GPX samples a human can upload through the real UI, pinned by tests, and push this change on its own as the first application of the per-change flow.

Work Log:
- Confirmed repo state first: Phase 4 (Task ID 7) already committed `phase(4): reconstruction drawing editor` and pushed; clean tree; 0/0 ahead/behind; dev server healthy (GET / 200s).
- Reused mulberry32 from src/features/gpx/fixtures/generators.ts as the single random source (no new PRNG) — the e2e generator only expresses single-gap perf files, so a dedicated small script was required for multi-gap manual samples.
- scripts/generate-demo-gpx.ts: deterministic demo-sample generator. QC Circle (Quezon City) runs at ~2.8 m/s, 1 Hz, gentle meander + pace noise, smooth elevation; recording holes = time jump + straight-line spatial jump (paused-watch signature). Outputs to download/: demo-qc-run-with-gaps.gpx (1260 pts, legs 420/480/360 s; hole 1 = 240 s + 550 m -> suspect time-gap; hole 2 = 1560 s + 2100 m -> severe time-gap) and demo-clean-run.gpx (900 pts, no anomalies).
- tests/demo-samples.test.ts: runs both samples through the REAL parseGpx + detectGaps (reuse of makeIo test helper). Asserts: with-gaps parses clean (no issues), 1260 points, exactly 2 gaps sorted severe->suspect with elapsedMs 1_561_000 / 241_000, implied speeds < 25 km/h (pure time evidence), anchors distinct; clean file = 900 points, zero gaps. Manual-testing expectations can never silently go stale.
- Verification: typecheck PASS; lint PASS; vitest 318/318 PASS (315 + 3 new).
- Committed and pushed as its own change immediately (per the user's clarified per-big-change push flow): `chore: demo GPX samples for manual testing`.

Stage Summary:
- Manual testers now have two small committed samples (download/demo-*.gpx) with pinned, printed expectations; regenerable via `bun scripts/generate-demo-gpx.ts`.
- Push workflow going forward: every big change = own commit, pushed as soon as it is green (not accumulated per phase). Phase 4 predates this clarification and sits on the remote as one commit; splitting it would require a history rewrite (force-push) — offered to the user, awaiting their call.
- Next: await user authorization for Phase 5 — Time & Pace Reconstruction.

---
Task ID: 9
Agent: Super Z (main agent)
Task: Strava compatibility verification. User reported concern that "only certain GPX formats can be understood" and attempted to supply two real Strava exports (processed "Export GPX" + "Export original") — but the attachments never reached the filesystem (upload/ empty; announced by the IM gateway, bytes never landed). Work: (1) audit the ingest path against documented Strava structures, (2) add permanent regression coverage for both Strava shapes, (3) request the real files again for direct verification.

Work Log:
- Audited the full ingest path: UploadZone (accept=".gpx,.xml", no MIME gate) → useGpxSession.loadFile (File#text, empty-file guard only) → parseGpx (namespace-tolerant localName structure navigation; BOM tolerated) → validateGpx → detectGaps. No size/MIME/format gates exist that could reject a well-formed Strava file.
- Confirmed no UI consumer of TrackMeta.type (captured verbatim, preserved on export) — Strava's numeric <type>9</type> activity code is harmless.
- Reuse check (rule 4): garmin-extensions.gpx already covers gpxtpx/gpxx point extensions + prefixed namespaces; prefixed-namespace.gpx covers fully-prefixed documents. NOT covered: the Strava-specific shapes — creator "StravaGPX", metadata with time but no name, numeric <type>9</type>; and a device-original with extensions AND a recording hole.
- Added two committed fixtures: strava-export.gpx (Strava processed export: StravaGPX creator, nameless metadata, type "9", ele+time-only points, 20 pts, 310 s hole -> suspect time-gap) and strava-original-garmin.gpx (device original: "Garmin Forerunner 265" creator, gpxtpx hr/cad on all 18 pts, type "running", 1810 s hole -> severe time-gap). Both use QC Circle geometry, 10 s smart-recording cadence, hole jumps below the speed-anomaly threshold (pure time evidence, like real paused-watch recordings).
- Registered both in PARSEABLE_FIXTURES (corpus-wide identity round-trip now covers them: verbatim raw strings, export stability, no gpxr markers).
- tests/strava-formats.test.ts: full-pipeline coverage mirroring use-gpx-session exactly (parseGpx -> validateGpx -> detectGaps -> distance stats -> identity export -> reparse). Asserts: StravaGPX quirks (nameless metadata, type "9" verbatim, bare points), device-original quirks (type word, every point's gpxtpx extension snapshot with hr/cad), gap expectations (1 suspect 310_000 ms / 1 severe 1_810_000 ms, implied speeds < 25 km/h), stats > 400 m, round-trip preserves type code + extensions.
- Verification: typecheck PASS; lint PASS; vitest 326/326 PASS (318 + 6 new + 2 corpus round-trip additions).
- Committed and pushed as its own change: `test: strava export format regression coverage`.
- Push initially rejected (non-fast-forward): the sandbox environment was rebuilt between sessions and restored every file with +x mode bits, so the local demo-samples commit was re-created with identical content but polluted 100755 modes (different hash) while the remote kept the clean original. Resolved WITHOUT force-push: rebased the Strava commit onto the remote tip with clean 100644 modes (verified `git diff origin/main --summary` shows zero mode changes) and set repo-local `core.fileMode=false` so environment chmod drift can never pollute the index again. Safety branch backup-strava-rebuild kept the pre-rebuild commit until the push landed.
- Asked the user to re-attach the two real Strava files (never arrived) for direct verification against the actual bytes.

Stage Summary:
- Both documented Strava export shapes are now pinned by tests and pass the entire pipeline on first run — the app demonstrably understands standard Strava GPX exports. If the user's real files still fail, the cause is in their specific bytes (e.g. FIT renamed to .gpx, gzip from the bulk export, or a device quirk) and will be diagnosed from the actual file.
- Next: re-verify with the user's real files when re-uploaded; otherwise await authorization for Phase 5 — Time & Pace Reconstruction.

---
Task ID: 10
Agent: Super Z (main agent)
Task: Fix the real-world Strava compatibility failure. The user committed their two actual Strava exports to docs/ via GitHub web upload ("Add files via upload", 6217d9e): strava_gpx_export.gpx (Strava processed) and strava_gpx_original.gpx (device original from a GloryFit watch). Diagnosis + fix + regression coverage.

Work Log:
- Pulled the user's files and diagnosed both through the real pipeline (scratch vitest, since deleted): the processed export parses cleanly (320 pts, 3663 m, no issues, no gaps); the GloryFit original HARD-FAILS with `malformed-xml: unbound namespace prefix "gpxtpx"` at line 17 — the watch emits Garmin-style gpxtpx: hr/cad extensions with NO xmlns:gpxtpx declaration anywhere, a fatal XML namespace violation that makes every strict DOMParser reject the whole document. This was the exact cause of the user's "only certain gpx format can be understood" report.
- Implemented undeclared-prefix recovery in parseGpx (the single parse entry point): when the strict parse rejects the document AND the text uses prefixes it never declares, the missing xmlns declarations are injected into the root start-tag (quote-aware scanner; prolog/comment/DOCTYPE-aware root locator) and the parse retried. Prefix bindings: gpxtpx/gpxx bind to Garmin's canonical URIs (GloryFit clones Garmin's schema verbatim — the meaning every consumer assumes); unknown prefixes bind to a clearly-marked synthetic urn:gpx-repair-studio:undeclared-prefix:<name>. A successful recovery surfaces exactly one `undeclared-namespace` WARNING (new ValidationIssueKind) naming prefix+URI; a failed recovery keeps the ORIGINAL parsererror — recovery never masks genuine malformation. Data untouched: lat/lon/ele/time captured verbatim from the recovered document (asserted).
- Extended the model/UI minimally: "undeclared-namespace" added to ValidationIssueKind (domain.ts) + label in validation-report.tsx (the exhaustive Record forces honest UI coverage); exportGpx.ts known-normalizations documented that recovered bindings materialize on re-export (the exporter genuinely fixes the defect — the warning correctly disappears after export, unlike data-level issues).
- New committed fixture undeclared-prefix.gpx (GloryFit-shaped: GPX 1.0, no trk name, un-prefixed vendor trk extensions, undeclared gpxtpx hr/cad, 8 pts) registered in PARSEABLE_FIXTURES — corpus round-trip now covers the recovered shape. projectModel's issues projection filters the recovery warning (it reports an input-document defect the exporter repairs, not a data property — its disappearance is asserted explicitly in the dedicated suites instead).
- Tests: tests/gpx-undeclared-prefix.test.ts (6: recovery+verbatim values+warning, fixture parse+track extras, round-trip with no second warning + byte-stable export, synthetic-URN fallback for unknown prefixes, malformed-with-undeclared-prefix keeps original error, clean declared files never trigger recovery). tests/strava-real-files.test.ts (7: both real docs/ files through the full pipeline — parse/validate/detectGaps/stats/export; GloryFit: 320 pts, hr+cad snapshots on all points, exactly one warning naming gpxtpx+Garmin URI, same ~3.7 km as the processed export; export: no findings, track name, stable round-trip).
- E2E e2e/strava-real-files.spec.ts (2): the user's exact complaint scenario through the real UI — GloryFit original uploads, shows summary (GloryFitPro, 320 pts) + "1 warning · Undeclared namespace prefix · gpxtpx" in the validation report + 3.6x km stats + rendered map, NO error; processed export uploads with "No problems found". Screenshot: download/strava-gloryfit-recovered.png.
- Environment handling: the rebuilt sandbox reaps background processes between tool calls — the dev server is now started inside the same invocation as any browser/E2E verification ((bun run dev &) + wait + run). agent-browser's localhost-vs-IPv6 quirk bypassed by using Playwright (which the suite already uses).
- Verification: typecheck PASS; lint PASS; vitest 340/340 PASS (326 + 14 new); E2E 24/24 PASS (22 + 2 new, serial); both real files verified end-to-end in Chromium.
- Committed and pushed as its own change: `fix: recover undeclared namespace prefixes (GloryFit/Strava original exports)`.

Stage Summary:
- The user's actual GloryFit/Strava-original file — previously rejected as "Not well-formed XML" — now loads through the real UI with an honest warning, and the processed Strava export was confirmed clean all along. The parser's namespace-tolerance contract now covers the full observed real-world spectrum: default/prefixed/no namespaces + undeclared-prefix recovery.
- ARCHITECTURE REPORT (user rule §20):
  - Reused existing code: single parse entry point (parseGpx — recovery lives inside it, no second parser); single export path (exportGpxIdentity — recovered docs re-export through the normal snapshot mechanism, no special-casing); ValidationIssue plumbing end-to-end (issues array → validate merge → validation-report labels); corpus + round-trip suite conventions; e2e upload helper pattern.
  - New modules: none — the change is a contained extension of parse.ts (two pure string-level helpers: findUndeclaredPrefixes, injectNamespaceDeclarations) plus one issue kind and one UI label. No new files outside tests/fixtures.
  - Reuse decisions: recovery is string-level pre-parse repair INSIDE parseGpx rather than a wrapper — every existing and future consumer (use-gpx-session, tests, Phase 7 export) gets it for free; Garmin canonical URIs chosen over per-file synthetic URNs for known prefixes because that matches what every ecosystem consumer (Strava, Garmin Connect, GPSBabel) assumes; the warning disappears on export by design (the exporter materializes bindings) — documented in both module contracts.
  - Duplication check: one prefix-scanner, one injector, one binding table, one issue vocabulary entry, one label — grep-verified no other DOM/XML repair logic exists anywhere; validation-report remains the single renderer of issue kinds (Record<ValidationIssueKind, string> exhaustiveness enforced by the type checker).
  - Architecture impact: the parse contract's tolerance envelope is now explicitly documented and test-pinned for undeclared prefixes; Phase 7 (merge/export) inherits correct handling of recovered documents automatically via the same snapshots; the real-file suites (unit + E2E) establish the pattern for pinning user-supplied samples under docs/ as regression corpus.
- Next: await user authorization for Phase 5 — Time & Pace Reconstruction.

---
Task ID: 11
Agent: Super Z (main agent)
Task: Fix the two real-user-reported issues on their Strava files: (a) the broken layout on the GloryFit original, and (b) the design flaw that repair/draw tools only appear when gaps are DETECTED (user: "shouldn't the purpose of this app is to fix and add missing points or draw missing area in every way"). User approved the plan ("okay go ahead").

Work Log:
- Diagnosed the layout bug empirically (Playwright probes, not theory): the undeclared-namespace warning embeds xmlns:gpxtpx="http://www.garmin.com/xmlschemas/..." — one unbreakable token that inflated the validation card's min-content, which (via grid min-width:auto chains) pushed the whole mobile page to 614px on a 390px viewport. A second, independent offender: the stats table's intrinsic 338px min-content (nowrap provenance badges) blowing the single-column mobile track to 388px. Chased a red herring en route: the panel-grid class appeared malformed in terminal output — hexdump proved the file was correct (the display layer was eating the "[m" sequence).
- Commit 1 (c4ae34e) "fix: mobile layout overflow from unwrappable tokens": overflow-wrap:anywhere (NOT break-words — only 'anywhere' participates in min-content sizing) on validation issue messages + summary value cells; PanelGrid columns set min-w-0 on their cards so wide content (the stats table) scrolls inside its own overflow-x-auto wrapper instead of the page. E2E regression pin: GloryFit upload at 390px asserts scrollWidth <= clientWidth. Verified 390=390 and 1440=1440; desktop 3-column template unchanged; pushed.
- Commit 2 "feat: manual repair spans (draw anywhere)": the design change. Detection is now a suggestion, never a gate:
  - Domain: GapKind += "manual"; ManualSpan { id, beforePointId, afterPointId } — the id uses the SAME deterministic scheme as detected gaps (gap/<before>/<after>), so a manual span over a detected boundary deduplicates into that gap's editor session and reconstruction for free.
  - editor-store: manualSpans + pickMode state; startPickMode (replaces an open editor), cancelPickMode, addManualSpan (creates span + opens editor, idempotent per boundary, withdraws stale skip marks), removeManualSpan (drops span + reconstruction + skip mark + closes its editor; no-op for detected-gap ids). Prune contract: the draw hook now passes detected ∪ manual ids — manual repairs survive threshold changes.
  - MapController: span-pick session (startPickSession/endPickSession) — click 1 sets the anchor (rendered as an emerald-ringed marker on a new gpxr-pick-anchor source/layer), click 2 on a different same-track point reports the pair; Esc cancels (window keydown, cleaned up on end/destroy); double-click zoom disabled while picking (a hurried second click must not zoom), pan/wheel-zoom stay available to travel between anchors; pick clicks never trigger gap selection; style swaps re-apply the session. Test bridge exposes pickSession {active, hasAnchor}.
  - buildRouteView 4th param manualSpans: manual spans NEVER split the recorded line and NEVER draw an unknown span (the stretch IS recorded — the user redraws by choice); they contribute boundary markers (the recorded↔authored seams) and render their committed reconstruction ON TOP of the recorded line (both stay visible: immutable original + user's claim). Detected-gap ids win over manual clones (defensive dedupe). Manual spans with unusable anchors yield nothing (honest hole).
  - use-draw-editor: pointIndex (id → point/segment/ordinal/track); manualRows joined into full GapRows (kind manual, severity info, impliedDistanceM diagnostic); activeGap/statusById/counts now over detected ∪ manual; the pick-session effect builds targets (usable points only) and fixes document order of the picked pair before addManualSpan.
  - UI: ManualRepairsCard (always mounted in the findings column — "New repair span" button, pick instructions, rows with status/Edit/Remove; hides ids currently shown in the detected list); GapList empty state cross-links "Something still looks wrong? Draw a repair manually"; MapCanvas shows a pick-mode chip; DrawEditorPanel hides the severity badge for manual spans and swaps "Mark as skipped" for "Remove repair span". The map highlight chip deliberately stays a detected-gap-only affordance (its "path was not recorded" copy would be a lie for manual spans).
  - Tests: editor-store-manual-spans (11), manual-repairs-card RTL (7 incl. GapList cross-link), map-route-view manual cases (4: no split/no span/markers, committed recon path anchors-included, detected-dedupe, unusable anchors), fixture extensions for the two binding-level suites. E2E manual-repair-span (3): full pick→draw→commit on the demo CLEAN file (zero gaps detected — the user's scenario; immutability of recorded stats asserted), Esc cancel, empty-list cross-link. Real-files E2E now asserts the GloryFit file shows "No gaps detected" AND the begin-pick button.
- Verification: typecheck PASS; lint PASS; vitest 362/362 (340 + 22); E2E 28/28 (25 + 3); live Playwright run on the user's REAL GloryFit file: desktop + mobile zero overflow, "0 candidate repair sites" + manual card, pick points 80→240 → editor "Manual repair/Editing", live badge 248 m, commit → card "Reconstructed", 1 reconstruction line rendered (screenshots: download/real-file-manual-drawing.png, real-file-manual-committed.png).

Stage Summary:
- Both user-reported problems fixed and pinned: layout (URI wrap + grid min-width hygiene, E2E-asserted at 390px) and tool gating (repairing is always reachable — detection demoted to a helper).
- ARCHITECTURE REPORT (user rule §20):
  - Reused: the entire Phase-4 editor machinery (drawModel commands, undo/redo, snap, resample, controller draw session) works on manual spans unchanged because everything is keyed by the same GapId; gap-vocabulary/status joins extended, not duplicated; isUsableStatsPoint stays the single usability predicate; test-bridge + pollBridge E2E patterns; makeBinding fixture conventions.
  - New modules: manual-repairs-card.tsx (the only new component); ManualSpan + GapKind "manual" in the domain; pick session inside mapController (no new lib/map file — interaction layer follows the draw-session seam).
  - Reuse decisions: order-fixing of the picked pair lives in the hook (lib/map stays file-order-agnostic); markers rendering extracted into a local renderMarkers helper inside buildRouteView to serve both detected and manual loops with one implementation; the pick anchor reuses the draft-handle visual language (emerald ring + white fill).
  - Duplication check: one pick-target builder, one manual-row join, one span id scheme, one status vocabulary; grep-verified no other XML-repair or pick logic exists.
  - Architecture impact: Phase 5 (time reconstruction) and Phase 7 (merge/export) consume manual spans through the same reconstructions-by-GapId map — Mode B semantics for a manual stretch (replace the in-between originals) will need explicit user-facing wording later; the E2E clean-file flow is the template for "repair without detection" coverage.
- Committed and pushed as its own change: "feat: manual repair spans — draw tools on every activity".
- Next: await user feedback; Phase 5 (Time & Pace Reconstruction) still awaits authorization.

---
Task ID: 12
Agent: Super Z (main agent)
Task: Session continuation. User asked (Q&A mode) how the anchoring/drawing interaction works for extending routes at either end, then asked for a detailed test script they can run themselves ("I will tell you what is really happening"). No new product code was requested in this turn.

Work Log:
- Re-established context after context-window continuation: verified repo state — local main = origin/main = 6cde45c ("feat: manual repair spans — draw tools on every activity"), tree clean, both real Strava files present under docs/ (Tasks 10-11 already committed and pushed: undeclared-prefix recovery, mobile layout overflow fix, manual repair spans).
- Read ManualRepairsCard + MapController pick-session code to extract exact UI labels for the user test script ("New repair span", "Cancel picking", pick instructions copy, "Draw route"/"Edit route", "Remove", GAP_KIND_LABELS manual).
- Diagnosed preview availability: sandbox reaper kills ALL user-spawned processes between tool calls (proved empirically: setsid+nohup marker process and direct `next dev` both dead by the next invocation; no cron/systemd/supervisor; passwordless sudo unavailable; platform-managed Caddy PID 2 on :81 returns 502 when :3000 is down).
- Verified delivered HEAD health in the rebuilt sandbox: `tsc --noEmit` PASS; vitest 362/362 PASS (23.5s); live smoke test `next dev` on :3000 → HTTP 200 with correct <title>GPX Repair Studio</title>. Cleaned experiment artifact dev-alive.log; tree remains clean.
- Called Complete(project_type="web_dev") to formally finalize/deliver the web project (platform preview provisioning) — first Complete call of the project per current records.
- Answered the user with a detailed click-by-click test script (below) rather than implementing new features: the two-anchor manual-span flow is LIVE; the one-anchor end/start extension (user's "<====---->" scenario) is NOT yet built and was flagged honestly as the next work item.

Stage Summary:
- Delivered state re-verified green (typecheck, 362/362 unit, live 200) and formally finalized via Complete.
- Preview serving constraint documented: user-facing preview depends on platform provisioning post-Complete; within-sandbox background processes cannot persist between tool calls.
- User test script issued covering: real-file loading (GloryFit recovery warning, processed export clean), layout integrity (desktop + narrow), manual repair span pick→draw→commit on a zero-gap file, detected-gap flow regression on demo-qc, undo/redo/cancel/Esc, remove span, export round-trip.
- Next: (a) user runs the script and reports findings; (b) one-anchor end/start extension (open-segment drawing) proposed as the next build, awaiting user go-ahead alongside Phase 5 (time & pace reconstruction).

---
Task ID: 13
Agent: Super Z (main agent)
Task: User question during live testing of manual repair spans: after picking anchors 0/1 and clicking drawn vertices, lines appeared connecting BOTH anchors to the first clicked vertex (V shape), then chain growth 0→2→3→1 — "is it supposed to be like this?" Answer-only turn, no implementation.

Work Log:
- User's pasted screenshot (pasted_image_1790300703386.png) never reached the filesystem (upload/ empty — same gateway non-delivery as the earlier GPX attachments); proceeded from the user's ASCII diagrams.
- Verified intended behavior against code: #draftPathPoints (mapController.ts) always renders the draft as [anchor_before, ...vertices in click order, anchor_after]; #applyRubberBand trails from the path end (anchor_after) to the cursor. The user's V-shape ([0,2,1]) and chain ([0,2,3,1]) diagrams are exact matches for the live full-path preview design — confirmed intended, not a bug.
- Answered with the mental model (complete-path preview, honest distance badge, closing segment becomes real on commit) and offered two UX polish options (dashed/lighter open segment; mouse trailer from last clicked vertex instead of far anchor) — NOT implemented, awaiting user preference.

Stage Summary:
- First real user test session of manual repair spans: pick flow understood and executed correctly; drawing flow works; the live full-path preview caused understandable confusion ("app drew lines I didn't click").
- Candidate polish backlog (user-dependent): visually distinguishing the open/closing segment from committed clicks. One-anchor end/start extension and Phase 5 still pending authorization.

---
Task ID: 14
Agent: Super Z (main agent)
Task: Implement the user's interaction redesign proposed during live testing: "click 1 point in the recorded route and after that click anywhere in the outside recorded map… what its see while editing is what they will see afterwards." Two commits.

Work Log:
- Commit 6b939e6 "fix: WYSIWYG draft rendering": the draft line previously rendered the full path (before → vertices → after) in one style, so the not-yet-placed closing segment and the far-anchor rubber band read as lines the app drew by itself (the user's confusion report). Now: the solid draft line is exactly the user-placed chain; the connection to the after-anchor is a distinct subdued dashed segment (new gpxr-draft-closing source/layer); the rubber band trails from the LAST placed point; midpoint handles exist on chain legs only; committed reconstructions render SOLID emerald (solid while drawing, solid after commit — style never changes under the user's feet; legend updated). Test bridge exposes chainCoordinates/closingCoordinates; e2e pins the split. 363/363 unit, draw-editor + manual-repair e2e green.
- Commit 0b75f14 "feat: one-anchor route extension": the two-click requirement is gone.
  - ManualRepairsCard offers two tools: "Add missing route" (primary, ONE click) and "Redraw a stretch" (the two-click selection kept for replacing a bad middle stretch).
  - The picked anchor's position derives the span shape (route start → open "before" head; route end → open "after" tail; mid-route → insert at [anchor, next] with the SAME id scheme as a picked pair, deduping into existing repairs).
  - Domain: ManualSpan union (replace | insert | extend); GapKind "manual-insert" ("Added route"); gapIdEnd/gapIdStart id schemes for open spans.
  - Open-ended editing: draw session with NULL far anchor — no closing segment ever, distance = exactly the drawn chain, straight-line warning never applies, committed reconstruction = anchor → vertices.
  - Pick hardening (found empirically): the demo tail's last two points sit 0.76 px apart at fit zoom; a sub-pixel camera shift made a click at the endpoint resolve to its neighbor (spurious 3 m insert). Segment endpoints now win pick near-ties within 2 px (ENDPOINT_TIE_PX).
  - Selection no longer refits the camera for open extensions (mid-draw camera swings steal click targets).
  - buildRouteView renders extend spans: ONE seam marker, committed chain over the recorded line, no unknown span. resample/drawModel/snap take optional far anchors.
- Debugging trail: probe spec revealed the tail-click neighbor bug (endpoint preference fix); probe revealed drawn-clicks-off-canvas twice — first a selection-triggered camera refit (removed for extensions), then the test's own fit bbox framing the wrong side of the tail (fixed). map-display:178 and draw-editor:137 each flaked once in full-suite runs and passed in isolation + subsequent full runs — environmental timing under dev-server load, not regressions.
- Verification: typecheck PASS; lint PASS; vitest 376/376 (363 + 13 new: store insert/extend contracts, open-ended drawModel/resample, route-view extend cases, panel open-end copy, card two-tool + open-row); e2e 29/29 (28 + 1 new one-anchor flow with WYSIWYG chain/no-closing assertions); live run on the user's REAL GloryFit original: tail extension span gap/t0s0:319/end, chain 4 / closing 0, stats unchanged (screenshots download/one-anchor-editor-open.png, one-anchor-drawing.png, one-anchor-committed.png; probe script persisted at scripts/verify-one-anchor-real.ts).

Stage Summary:
- The user's proposed interaction is now the product's primary repair flow: one click on any recorded point, then free drawing anywhere; WYSIWYG contract enforced at the rendering, geometry, distance, and commit levels; the two-anchor flow remains for stretch replacement with the same visual honesty.
- ARCHITECTURE REPORT (user rule §20):
  - Reused: the entire Phase-4 editor machinery (commands, undo/redo, snap, resample, controller draw session) works on insert/replace spans unchanged (same GapId keys); extend spans flow through the same session with a null far anchor — one optional-parameter change instead of a parallel editor; the pick session gained a mode instead of a second mechanism; test-bridge + pollBridge patterns.
  - New modules: none — ManualSpan variants and two id helpers extend the existing vocabulary; no new files outside tests.
  - Reuse decisions: shape derivation lives in the hook (lib/map stays position-agnostic); endpoint near-tie preference lives in the controller's pick scan where pixel distance is already computed; RepairRow (optional boundaries) is a supertype of GapRow so detected rows assign unchanged and every consumer is compiler-forced to face the open case.
  - Duplication check: one pick-target builder (endpoint-flagged), one span-id scheme family, one status vocabulary; grep-verified no other pick/derivation logic exists.
  - Architecture impact: Phase 5 (time reconstruction) must handle extend spans (no far anchor to interpolate toward — timestamps extend past the anchor; side "before" chains reverse into route order); Phase 7 (merge/export) decides Mode B placement for open chains (append/prepend at the anchor's segment boundary); the WYSIWYG rendering split (chain vs closing) is the template for any future path-preview feature.
- Next: user tests the new flow; Phase 5 (time & pace reconstruction) still awaits authorization.

---
Task ID: 15
Agent: Super Z (main agent)
Task: User confirmed the WYSIWYG one-anchor behavior ("YES THIS IS HOW IT SHOULD BEHAVE") and requested two adjustments: (1) road detection — click a first point and a far second point and the app finds the right road path between them, so clicks before/after a curve trace the curve; (2) draggable clicked points for adjustment. Implementation of road-follow drawing.

Work Log:
- Re-established state: Tasks 14's commits (6b939e6 WYSIWYG rendering, 0b75f14 one-anchor extension) were already live; local was ahead 1 (verification artifacts c958cf8) — pushed. Baseline had one latent typecheck error (manual-repair-span spec's local BridgeState missing lastCameraAction) — fixed. Baseline: 376/376 unit green.
- Read the full draw pipeline (mapController draw session, use-draw-editor orchestration, editor-store commands, drawModel, resample, geojson collections, use-map-controller buildRouteView, draw-editor-panel) to place the feature on the established seams.
- Design: legs = consecutive node pairs of [near-anchor, ...vertices, far-anchor?]. Road-follow resolves each leg's geometry from a public routing service; resolved legs live in a store SIDE TABLE keyed by gapId (never inside the undoable Reconstruction — §D-3); the store also carries roadFollow mode (car/foot/off, default car) + routing status (pending/failed). Privacy: only clicked leg endpoints are sent; the GPX never leaves the browser; disclosed in the panel.
- New module features/reconstruction/roadFollow.ts: WYSIWYG join (road interior stitched between the EXACT clicked nodes — providers snap waypoints onto the road, so first/last road points are replaced by the nodes; every rendered point is committed), per-leg distance-mid midpoints (on the road, not the chord), closing-leg geometry, isStraightLinePath over rendered points; RoadFollowRouter with INJECTED fetch (features/** are eslint-banned from global fetch), OSRM driving (router.project-osrm.org) + Valhalla pedestrian (valhalla1.openstreetmap.de) adapters, AbortController 6 s timeout, session cache + in-flight dedup keyed mode|rounded-endpoints, short-leg fast path (<10 m no request), every failure resolves null → straight fallback.
- MapController: DrawChainJoinFn/DrawClosingJoinFn INJECTED by the hook (the DrawSnapFn pattern — no domain code inside the map adapter). The solid draft line renders the joined road geometry; during a handle drag the overridden node matches no leg → straight live preview, road re-applies when the move commits and the hook re-routes. The dashed closing segment renders road geometry too (preview and commit share one join). Test bridge: renderedChainCoordinates added; chainCoordinates unchanged (clicked nodes). draftClosingCollection now takes coordinates.
- use-draw-editor: shared router singleton (browser fetch injected at the app layer); resolution effect derives the wanted leg set on every vertices/anchors/mode change, seeds cached legs synchronously, resolves missing ones async with a generation counter guarding stale results (vertex moved / gap switched / mode changed); legs pushed to the controller via updateDrawSession(vertices, joins). Distance badge + straight-line warning computed over the RENDERED path (road interiors count). Routing status in the store (avoids react-hooks/set-state-in-effect).
- resamplePath(…, roadLegs): matching legs contribute provider geometry as role "road" points with honest geodesic cumulative distances; spacing never re-densifies road legs. buildRouteView/ReconstructionRenderRef carries per-gap roadLegs so committed reconstructions render (and Phase 5/7 will export) the road geometry.
- Panel: "Between clicks, follow" segmented control (Roads / Footpaths / Straight lines), privacy microcopy, routing status line (Finding the road… / unavailable right now / Drag any point to adjust it — the road re-finds itself), drawn-points header teaches drag + double-click.
- Tests: tests/road-follow.test.ts (19: keying/lookup, join stitching incl. mixed chains + degenerate legs, midpoint-on-road, closing geometry, rendered-path honesty, OSRM/Valhalla request shapes + parsing, cache, in-flight dedup, short-leg, all failure modes → null); resample road cases (roles, no re-densification, monotonic true cumulatives, non-matching legs ignored); store side-table contract (defaults, setRoadFollow/setRoadLegs no-op, removeManualSpan/prune/reset); panel group (pressed states, intents, status transitions, off hides status, drag hint); map-route-view committed road geometry; draftClosingCollection new signature + verbatim road passthrough. 412/412 unit, typecheck + lint green.
- E2E: e2e/helpers/road-follow.ts (abortRoadRouting + mockOsrmBulge echoing request waypoints with a northward mid bulge, plus a call log); draw-editor + manual-repair-span specs stub the routing network in beforeEach (deterministic regardless of sandbox connectivity); new e2e/road-follow.spec.ts (4): road traced between clicks with exact-node bracketing + clicked chain untouched + badge measures the road (> chord), drag re-routes both adjacent legs (request log + new geometry), Straight-lines mode (rendered == nodes, zero requests), failed service → honest straight fallback + surfaced status. 33/33 e2e.
- Live verification on the user's REAL GloryFit original against the REAL public OSRM service (scripts/verify-road-follow-real.ts, no interception): one-anchor tail extension, 2 clicks → 2 OSRM requests OK → rendered chain 36 points (3 clicked nodes + 33 real road points), badge 1.06 km, status "Drag any point…"; dragging the point fired 2 more requests and re-routed (47 points). Screenshots: download/road-follow-real.png, road-follow-real-dragged.png.
- Committed as its own change and pushed: b1e7a2e "feat: road-follow drawing — clicks trace the real road, drags re-route".

Stage Summary:
- The user's two adjustments are live: clicks trace the real road between them (OSRM roads default, Valhalla footpaths optional, straight lines off-switch), and every clicked point is draggable with automatic re-routing of its two adjacent legs on drop.
- WYSIWYG contract extended to the road: the solid line, the distance badge, the straight-line warning, and the committed reconstruction all consume ONE join implementation; the clicked vertices remain the only truth (undo/redo untouched by network data).
- ARCHITECTURE REPORT (user rule §20):
  - Reused: the entire draw-session seam (DrawSessionOptions extension + updateDrawSession, not a parallel session); the DrawSnapFn injection pattern for the joins (map adapter stays domain-free); the store's transient-aid slot for mode/status; resamplePath/buildRouteView as the single committed-geometry pipeline; test-bridge + pollBridge + e2e upload helper patterns.
  - New modules: features/reconstruction/roadFollow.ts (pure join + fetch-injected router) and e2e/helpers/road-follow.ts; RoadFollowMode/RoadLeg in domain.ts; roadLegs/roadFollow/roadRouting in the editor store. No other new files outside tests.
  - Reuse decisions: the join lives in ONE place and is structurally injected into the controller (no lib→features import); routing status went to the store after react-hooks/set-state-in-effect correctly rejected setState-in-effect; provider defaults chosen for reliability (OSRM car = the stable keyless public demo; Valhalla pedestrian = best-effort with straight fallback).
  - Duplication check: one leg-key scheme, one join, one router, one side table, one status vocabulary; grep-verified no other routing/road logic exists.
  - Architecture impact: Phase 5 (time reconstruction) must interpolate timestamps across road points (resamplePath already hands it cumDistanceM along the true geometry); Phase 7 (merge/export) will bake road points into the exported GPX via the same resamplePath call sites; the privacy line ("GPX never leaves the browser") now has a documented, disclosed exception for clicked coordinates only.
- Next: user tests the road-follow flow; Phase 5 (time & pace reconstruction) still awaits authorization.

---
Task ID: 16
Agent: Super Z (main agent)
Task: User-requested UI overhaul: (1) map-first two-section layout — big map + tools in a side panel (section 1), stats and other info below (section 2, reached by scrolling); (2) clearer drawing vs dragging mode communication; (3) more micro-interactions and hover-to-learn use-case info on all tools.

Work Log:
- Surveyed the full presentation layer (app-shell, panel-grid, map-canvas/toolbar/legend/badge, draw-editor-panel, manual-repairs-card, header, stats-panel, summary/validation/segment cards), the mapController's cursor/drag/session wiring, and every test touching layout (inspection-flow, map-components, draw-editor-panel, manual-repairs-card units; all e2e specs' selectors/viewport assumptions).
- New WorkspaceLayout (layout/workspace-layout.tsx): section 1 "Repair" = viewport-tall map (lg:h-[calc(100dvh-12.5rem)], 65dvh mobile, min 400px) + sticky scrollable tools column (21/24rem); section 2 "Details" = 3-col card grid + stats; scroll cue chip + back-to-map link + sticky-header section nav (#repair/#details anchors). PanelGrid deleted. All three grids use minmax(0,1fr) tracks — the old PanelGrid's sideways-overflow guarantee, re-derived after a live 14px mobile overflow (404 vs 390) reproduced and fixed on the real GloryFit file (probe scripts, since removed).
- MapToolbar rebuilt as a vertical icon rail (right edge, vertically centered — clear of badge/pick chip/legend/gap chip): Draw/Pan group, basemaps popover (side=left), fit. Icon-only with sr-only labels; all interactive testids and the "Basemap provider" aria-label preserved (e2e untouched).
- HintTip (shared/hint-tip.tsx): Radix tooltip, ~450 ms deliberate dwell, title + use-case description + kbd accelerator. On the rail (Draw/Pan/Basemap/Fit), both ManualRepairsCard tools, and the three road-follow choices (per-choice use-case text). Snap + spacing got native titles.
- Mode clarity: (a) on-map mode chip (map-mode-chip, top-left; shows Drawing/Panning + D/P hint, click toggles); (b) DrawDistanceBadge slimmed to a pure measurement readout ("road length, not straight line"); (c) D/P keyboard accelerators in use-draw-editor (window keydown, gated on active session, ignores inputs/textarea/select/contenteditable + modifier combos).
- Controller (mapController.ts): handle drags now work in BOTH draw and pan modes (pointer-targeted edits — mousedown/#onMouseMove drag path hoisted above the draw-mode gate); cursor honesty (crosshair drawing, grab over a handle, grabbing while dragging, restored per-mode on drop); feature-state hover scale on draft handles (radius 5.5→8, stroke 2.5→3.5) — geojson handle features now carry top-level ids; hover id tracked on enter since MapLibre mouseleave carries no features; style-swap and endDrawSession clear hover state via source re-set.
- RevealOnScroll (shared/reveal-on-scroll.tsx): CSS scroll-driven animation (animation-timeline: view(), entry 5%→55%) in globals.css, @supports + prefers-reduced-motion guarded, content always in the DOM (first attempt with IntersectionObserver+setState was correctly rejected by react-hooks/set-state-in-effect).
- Tests: map-components badge tests rewritten for the new readout (+2), mode-chip trio (+3), workspace-layout unit file (+5) = 420/420; e2e workspace-layout.spec.ts (+3: sticky tools column with map dominance, scroll-cue → details round-trip with in-viewport stats, header nav with exact:true to dodge the scroll-cue substring) and draw-editor mode-chip + D/P keys (+1) = 37/37. Fixed en route: my own probe used .maplibre-canvas instead of .maplibregl-canvas (map was fine all along).
- Live verification on the REAL GloryFit original (scripts/verify-qol-layout.mjs): desktop repair/editor/details + mobile screenshots (download/qol-*.png), VLM-inspected — map-first layout, mode chip "Drawing (D)", 4-icon rail, road-length badge, editor sidebar, no overlaps; mobile shows no horizontal overflow.
- Committed 32251b8 (on top of the platform's 327ed11 auto-commit of the early file creations) and pushed to origin/main.

Stage Summary:
- The workspace is now two screens as the user sketched: [MAP and tools] then [stats and other infos], with the map dominant and every repair tool reachable without scrolling; stats live one scroll (or one click) away.
- Drawing vs dragging is answered three ways at once: the mode chip (always-visible state + toggle), the cursor (crosshair/grab/grabbing), and the toolbar/kbd accelerators; dragging a placed point no longer requires leaving pan mode.
- Tools teach themselves: dwell-hover hints with concrete use cases on every map tool and repair tool; the details section fades in on scroll (CSS-only, reduced-motion safe).
- ARCHITECTURE REPORT (user rule §20):
  - Reused: the composition-root pattern (AppShell slots), the §F minmax(0,1fr) overflow discipline, the DrawSession seam (drag-in-pan rides the existing mousedown/mousemove path with the mode gate narrowed, not a second mechanism), feature-state styling inside the existing layer registry, e2e testid/aria contracts (only additive changes).
  - New modules: workspace-layout.tsx, hint-tip.tsx, reveal-on-scroll.tsx (+ CSS in globals.css); PanelGrid deleted. No new logic below the presentation layer except the controller's hover/cursor/drag-gate edits.
  - Reuse decisions: keyboard accelerators live in use-draw-editor (session-scoped, form-safe) rather than the controller (which never owns window listeners except pick-Esc); the mode chip is presentation-only and calls the existing setDrawMode intent; scroll-reveal is CSS so react-hooks/set-state-in-effect stays satisfied.
  - Duplication check: one tooltip component, one mode chip, one rail; grep-verified no leftover title-only hints duplicated on HintTip-wrapped controls.
  - Architecture impact: Phase 5 (time & pace) panels slot into the tools column unchanged; the sticky column's max-height/scroll budget grows as editor cards gain content; mobile single-column order (map → editor → manual → gaps → cue → details) is now a pinned e2e/mobile contract.
- Next: user tests the new layout; Phase 5 (time & pace reconstruction) still awaits authorization.
